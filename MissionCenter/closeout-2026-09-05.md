# CloudHime 2026-09-05 修復／Rust 第一階段交付

## 結論：部分交付；嚴格完成閘門 BLOCKED，不是 PASS

基線：`652ca08f6d14d3a3d4e3e029f366ec918f3309c9`（main）。
原實作階段曾因工具能力誤判而未能推送；後續工作階段已確認 GitHub Git Data 寫入能力可用，並依使用者要求直接提交 main。終端 Git CLI 仍無法解析 github.com，因此發布採 GitHub connector 完成。
提交訊息使用 `[skip ci]`，依 GitHub 官方規則跳過由 `push`／`pull_request` 事件觸發的 Actions；本輪不主動執行 CI。
這份附件是既有 CH-T107／CH-T112 的實作與驗證證據，不是新的任務 lifecycle source。
`tasks.md` 與 managed progress 未被改成 Done；沒有宣稱全專案無 P2 以上問題。

## 已實作

1. 修正 dependency-contract 安裝到 venv 卻呼叫 system Python 的四個驗證命令；MSIX fixture 先安裝與既有 lock 相同、帶 SHA-256 的最小 packaging 工具依賴。沒有改 workflow triggers、permissions 或刪除 release gates。
2. `.gitattributes` 對鎖定 benchmark JSON／兩支 evaluator 保留原始 Git 位元組。`translation_e2e_cases.json` 原始 LF SHA-256 為 `ae3d0aa535fd2e3518410ccb8f39da4e32ed26f5866f9163f80e22c0f0ca538a`，與 lock 相符；轉成 CRLF 才不符。未更新 lock、資料或品質門檻。
3. 五個明確指名的外部圖庫測試在圖片不存在時顯式 skipped，理由包含「not quality evidence」。`--require-external-corpora` 將缺圖提升為 collection error；manifest 不見、JSON 不合法或路徑越界不會被 skip。真實 benchmark/evaluator 的 fail-closed 邏輯未改。
4. Qt 測試防護改為需要時才載入，防護仍保持 session lifetime，避免延遲熱鍵 callback 跑出測試範圍。純核心測試不再由這份 fixture 強迫載入整套 UI。
5. FrameGate 拆出獨立 metrics 模組；uint8 差值改用 int16 並就地取絕對值，移除已脫離原圖的 sample 重複複製，拒絕零通道輸入。context／shape／dtype 隔離與 `skip_ocr=False` 不變。
6. 新增 Rust 1.98.1、零外部 crate 的 native 核心，提供有界 C ABI、版本檢查、錯誤碼及 Python adapter。原生路徑預設關閉；只有 `CLOUDHIME_NATIVE_FRAME_METRICS=1` 才嘗試固定專案路徑的動態庫。不可用時回到 NumPy，不下載、不自動編譯。
7. 新增四支回歸測試，加入原有五組 test inventory；既有測試檔未移除。來源／選型紀錄及使用者的逐字嚴格審查提詞保存在 `reviews/`。

## 本輪實際執行的證據

環境：Linux，Python 3.13.5，NumPy 2.3.5。只重建本輪必要的來源檔，原始檔內容先核對 Git blob SHA；不是完整 Windows checkout。

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q \
  tests/test_frame_gate.py tests/test_frame_metrics.py \
  tests/test_native_frame_metrics.py tests/test_corpus_policy.py \
  tests/test_stabilization_contracts.py
```

最新結果：**99 passed, 1 skipped in 2.97s**。
唯一 skipped 是「實際 Rust 動態庫對 NumPy」測試；未啟用 native，不能算 Rust 通過。
包含原有 FrameGate 測試、數值差異、全部 uint8 值組合、非連續 view、寬整數、
FFI Python 邊界的 mock、實際 Git autocrlf checkout、實際 pytest 子程序的缺圖／strict／不載入 Qt 檢查。
Mock ABI 僅驗 Python 包裝層，不等於已編譯 Rust。

`python -m compileall -q frame_gate.py frame_metrics.py native_frame_metrics.py ci tests native/verify.py` 通過。
`git diff --check` 通過。Workflow 只做靜態結構與路徑契約測試，沒有執行 Windows jobs。

`python native/verify.py` 實際回 exit 2：

```text
BLOCKED: rustup and installed Rust 1.98.1 are required; nothing was downloaded.
```

CodeRabbit prerequisite 實際失敗：CLI 不存在；安裝腳本下載失敗：
`curl: (6) Could not resolve host: cli.coderabbit.ai`。
**沒有啟動獨立專家／CodeRabbit review；不得將主代理測試或自查改稱獨立審查。**
專用提詞已保存，但保存提詞不等於執行審查。
Mission Center／Impeccable 的 source skill 已閱讀並用於切片、取捨與 gate 設計；兩者 CLI／detector 未執行。

## 有界效能觀察

`reviews/2026-09-05-frame-microbenchmark.json` 保存 28 筆 CPU-only synthetic frame measurements：
64x64 RGB、兩種 baseline/candidate 順序、各七次、每 sample 1000 次 observe。
中位數 baseline 176.867427 us、candidate 158.3181015 us（約減少 10.5%）。
這只是本環境 Python/NumPy 幀觀察微測試；不是 Rust 加速、Windows 成績、GPU 成績或端到端翻譯提升。
不據此自動切換 production native default。

## 尚未通過的 P2 以上項目／後續精確範圍

下列為本輪問題分級與既有任務對照，不覆寫 tasks.md 的 canonical priority/status。

| 優先度 | 既有任務 | 未解決事項／完成條件 |
| --- | --- | --- |
| P1 | CH-T112 | 發布步驟已在後續工作階段直接提交 main；仍需完成剩餘獨立審查與 Windows／Rust 驗證，禁止把「已推送」等同「已通過完成閘門」。 |
| P1 | CH-T112 | 缺 Rust 1.98.1 編譯、fmt、Clippy、真正 ABI 差異證據；在有精確工具鏈的環境執行 `python native/verify.py`，通過前 native 保持預設關閉。 |
| P1 | CH-T112 | 獨立嚴格審查未執行；修復 CodeRabbit CLI 網路／安裝／授權，再按已保存提詞做真實 review→fix→受影響測試。不得標 PASS。 |
| P2 | CH-T115 | 既有 Windows UI 窄欄案例 content 423px > viewport 337px。本輪未修改或渲染此 UI；需重現 sizeHint／layout 約束並驗證縮放與主題。 |
| P2 | CH-T114 | 既有 llama-server Bearer key process argv 暴露未修；需安全移除命令列 secret，維持啟停／清理與 loopback 驗證契約。 |
| P1 | CH-T112／CH-T64 | 完整 Windows inventory、真實 OCR/API、GPU、frozen build、MSIX 安裝與乾淨機器驗證未跑；可在本機跑，不要求啟用 GitHub CI。原本兩個 real-release jobs 是手動 opt-in，不應把 skipped 誤報成下游被紅燈阻塞。 |
| P2 | CH-T115／CH-T64 | 背景資產再散布授權未確認；正式發行前仍需明確證據。 |

不能用「99 passed」覆蓋以上未驗證事項，也不能把外部 corpus skipped 計入品質通過率。
