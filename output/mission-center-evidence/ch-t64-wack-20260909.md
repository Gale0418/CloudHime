# CH-T64 本機 optional WACK 實測

- 使用 2026-09-06 既有 `dist/CloudHime-0.1.0.0-x64.msix` 的隔離副本，不代表後續工作樹 fresh build。
- 修正 wrapper PS5.1 report parent expression 後，短效開發簽章、SignTool verify、安裝／AUMID 啟動／卸載成功。
- `output/signed-msix-gate.ps1 -RunWack`（Windows PowerShell 5.1）最終 exit 0，RESULT=PASS。
- 真實 appcert 完成，報告 `output/wack-ee8366c13d084636ba57b42652a1c40f.xml`，wrapper 驗證唯一根屬性 `OVERALL_RESULT=PASS`。
- finally 清理：package_count=0、stage_exists=False、source_unchanged=True；短效憑證只移除本次建立的兩個 store entry。
- 這是既有開發套件 optional local WACK PASS；不是 Partner Center certification、Store submission 或乾淨 Windows 首次模型下載驗收。
